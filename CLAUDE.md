# CLAUDE.md -- Memoire Projet

> Ce fichier est lu automatiquement par l'IA au debut de chaque conversation.
> Mets-le a jour a la fin de chaque session de travail.

---

## Objectif Final
Construire Plotline, un SaaS de gestion de personas IA pour createurs d'influenceurs IA sur Instagram et TikTok.

Le produit permet de configurer un ou plusieurs personas (identite visuelle, voix, piliers de contenu, style narratif, frequence de publication) puis de generer automatiquement des batchs de contenus coherents (image + caption + hashtags), avec validation humaine avant publication.

---

## Stack Technique
- Nuxt 3 monorepo (Vue 3 front + server routes back)
- Prisma + PostgreSQL
- BullMQ + Redis
- SDK Anthropic JS (claude-sonnet-4-6)
- Deploy VPS Hostinger
- Domaine cible: plotline.sassify.fr
- Email: SMTP Hostinger via variables d'environnement

---

## Conventions IA et Code
- Toute la logique serveur vit dans server/utils et server/api.
- Les utilitaires dans server/utils sont des fonctions pures exportees.
- Le format de sortie de Claude est toujours du JSON brut, sans markdown.

---

## Etat Actuel du Projet
**Phase** : Produit fonctionnel en production, consolidation du pipeline de generation
**Derniere session** : 2026-09-16
**Progression globale** : 80%

### Ce qui est fait :
- [x] Socle technique: Nuxt 3, Prisma + Neon, auth complete (session, reset mot de passe, changement email)
- [x] Connexion et inscription via Google OAuth (echange fait a la main, sans module tiers ; recette complete dans `docs/recette-connexion-google.md`)
- [x] Multi-profils avec trois types de compte: INFLUENCER_CREATOR, CONTENT_CREATOR, BRAND
- [x] Identity lock: face ref 3 panneaux, prompts corps/cheveux verrouillables
- [x] Generation image (Gemini) et video (Veo, Kling, Seedance) avec selection de modele
- [x] Stockage partage Vercel Blob pour tous les medias, avec repli local
- [x] Flux editorial: PROCESSING -> PENDING -> VALIDATED -> PUBLISHED
- [x] Historique des versions avec retour arriere sans perte de media (5 images / 2 videos)
- [x] Carrousels: slides regroupees, navigation, modification isolee d'une slide
- [x] Format decide par le prompt, arbitrage seulement quand le prompt ne dit rien
- [x] Campagnes, dashboard bento, calendrier mensuel, Studio
- [x] Publication Instagram (Meta Graph API) et X/Twitter
- [x] Script de nettoyage des medias orphelins (dry-run par defaut)
- [x] Publication automatique des contenus a leur date planifiee (opt-in via `SCHEDULER_ENABLED`)
- [x] Lien marque <-> ambassadrice en plusieurs-a-plusieurs (table `BrandAmbassador`), rattachement editable depuis la fiche du profil
- [x] Planificateur editorial: cadence par profil, idees redigees par Claude avec repli deterministe, revue avant generation
- [x] Genre du persona (Femme/Homme) avec silhouettes et descriptions corporelles masculines dediees dans la generation d'image
- [x] Comparaison de plusieurs fiches reference visage a la creation d'un persona: jusqu'a 3 generees a partir de photos differentes, comparees cote a cote avant de choisir, remplacement de la moins bonne au-dela de 3
- [x] Systeme de widgets Studio fusionne sur `main` (branche `feature/studio-widgets`): prompts pre-remplis par template pour 4 cas d usage (portrait, vlog, food ad, UGC produit)
- [x] Endpoint externe `/api/external/video-jobs` fusionne sur `main` et deploye: home.sassify.fr peut pousser des jobs video vers le pipeline Plotline
- [x] Widget Studio "Video Scenario" (`SCENARIO_BLOG`): idee libre -> scene + script, persona choisie comme simple proprietaire du contenu par defaut, case "Verrouiller l'identite" optionnelle (desactivee par defaut) qui bascule Omni Flash entre flux 1 tour (libre) et 2 tours (identite verrouillee, defauts connus). Reutilise l historique de versions existant pour comparer les modeles plutot que de construire une UI de comparaison dediee.
- [x] Assistant Claude generique "idee -> prompt" dans le Studio: une zone "exprime ton besoin" + bouton dans les 4 endroits ou l utilisateur ecrivait un prompt a la main -- prompt libre (image/video, `server/utils/promptAssistGenerator.js`), champs de n importe quel widget (`server/utils/widgetFieldsAssistGenerator.js`, pilote par un `assistHint` par variable dans `server/data/widgets.js`), et slides de carrousel ou Claude decide lui-meme du nombre de slides entre 2 et 10 selon l idee (`server/utils/carouselAssistGenerator.js`). Le widget "Video Scenario" est migre sur ce mecanisme generique (son ancien generateur dedie, `scenarioScriptGenerator.js`, est retire) : ses consignes de champ (scene en anglais, script 15-25 mots en francais) vivent desormais dans `assistHint`, pas dans un fichier a part. Eprouve en navigateur (2026-09-16) sur les 4 zones, avec et sans persona verrouille -- la description du persona ne s injecte dans le prompt genere que quand l identite est reellement verrouillee.
- [x] Attribution d'un persona/profil rendue optionnelle pour generer du contenu (2026-09-16) : `GeneratedContent.influencerId` reste une colonne obligatoire en base, mais `server/utils/defaultProfile.js` (`getOrCreateDefaultProfile`) cree et reutilise un profil `ACTIVITY` neutre "Contenus sans persona" par compte des que `influencerId` est absent de la requete (`/api/generate/image`, `/api/generate/video`). Cote Studio, le picker de profil des widgets qui ne verrouillent pas d identite (Food Ad, Video Scenario) devient explicitement optionnel ; le flux Prompt libre ne bloque plus "Generer" quand le compte n a encore aucun profil.
- [x] Reglage de la cadence editoriale et liste des plans passes (2026-09-16) : `postsPerWeek`/`formatRotation`/`publishHour` (deja en base, jamais exposes) sont desormais exposes et modifiables sur `GET`/`PATCH /api/profiles/:id` et editables depuis une section dediee de la fiche persona (`app/pages/profiles/[id]/edit.vue`), en reutilisant les normalizers deja purs de `server/utils/contentPlanner.js`. Nouveaux endpoints `GET /api/plans` (liste par profil ou tout le compte) et `POST /api/plans/:id/discard` (DRAFT -> DISCARDED, calque sur `approve.post.js`), qui alimentent une section "Plans precedents" sur la fiche persona et un bouton "Jeter ce plan" sur l ecran de revue (`app/pages/plans/[id].vue`). Les idees d un plan DRAFT pas encore generees (`keep && !contentId`) sont fusionnees dans le calendrier mensuel (`app/pages/calendar.vue`) avec un style visuellement distinct (bordure pointillee) et redirigent vers l ecran de revue du plan au clic plutot que d ouvrir le tiroir de detail habituel. Eprouve de bout en bout en navigateur (2026-09-16) : modification de cadence persistee, plan cree puis visible sur le calendrier et dans "Plans precedents", abandon depuis les deux endroits, second abandon refuse en 409.

- [x] Video longue Omni Flash sans face ref (2026-09-19, non deploye/commite) : `requestOmniFlashMultiSegment` (`server/utils/videoGeneration.js`) decoupe le script en phrases (`server/utils/scriptSegmentation.js`, ~22 mots max/segment, 4 segments max = plafond Google de 40s) et enchaine les segments via `previous_interaction_id`; il se declenche tout seul des que le script depasse un segment, pour tout appel Omni Flash sans face ref (`/api/external/video-jobs` et widget Video Scenario), et le chemin 1 segment reste inchange. Chaque interaction renvoie la video CUMULATIVE : seule la sortie du dernier segment est gardee (aucune concatenation). Au-dela de 4 segments, le script est tronque aux phrases qui rentrent (avertissement logue). Eprouve en reel : 4 segments, ~295s de generation, 40s de video, personnage et decor coherents. Cout ~4 generations Omni Flash par video longue.

### Prochaines etapes :
- [ ] Essai Kling image2video avec audio natif (persona + lip-sync FR) -- EN ATTENTE de recharge du solde API Kling (erreur 1102 "Account balance not enough" le 2026-09-21, rien depense cote Kling). Plan : 1 seul essai de 10s (`kling-v3`, mode std, `duration: '10'`, `sound: 'on'` -- nom du parametre audio NON confirme par la doc officielle, qui ne s affichait pas ; si le clip revient muet, ne pas retenter a l aveugle), face ref d un persona (ex. Melina, lecture seule) -> image de depart nette -> phrase francaise entre guillemets. Recherche deja faite : Kling 3.0 annonce l audio natif en 5 langues (zh/en/ja/ko/es), le francais n est PAS officiel (tests communautaires : "fonctionnel sur clips courts, inconsistant") ; 3 a 15s par generation ; "Element Voice Control" pourrait stabiliser la voix (non verifie cote API) ; tarif tierce partie ~0,084 $/s en standard sans audio, l audio semble ~doubler (a verifier dans la console). L API lip-sync separee (1 credit/s, video 2-10s) est le plan B (voix TTS generique). Ne pas presenter Kling comme moins cher : non demontre.
- [x] Faille corrigee (2026-09-21, non commite) : `POST /api/upload/face-ref` n exigeait ni session ni propriete du profil -- quiconque connaissait un identifiant de profil pouvait remplacer sa face ref. Desormais `requireAuthUser` + profil du compte (`findFirst` avec `userId`) sur les branches JSON et multipart, les refus gardant leur code (401/404) au lieu d etre aplatis en 500. Eprouve : 401 sans session sur les deux branches, 200 sur son profil (les 4 ecrans appelants passent tous par la branche JSON avec la session), 404 sur un profil inconnu. Le cas "profil d un autre compte reel" n a pas ete teste faute de second compte, mais c est la meme requete `userId`.
- [x] Faille corrigee (2026-09-21, non commite) : `POST /api/generate/face-ref` n avait pas d authentification alors qu il appelle Gemini (credit depensable par n importe qui). `requireAuthUser` ajoute (via `await import`, le fichier est en `module.exports`), 401 conserve. Eprouve : 401 sans session, requete authentifiee validee jusqu au 400 "sourceImageBase64 requis" sans appel Gemini (aucune generation reelle lancee pour ce test). Les 4 ecrans appelants utilisent `$fetch` de meme origine (session envoyee).
- [x] Balayage securite complet en cascade (2026-09-21, non commite) : le balayage de tous les endpoints sans reference d authentification (il n existe AUCUNE garde globale cote serveur, `server/middleware` n existe pas) a revele et fait corriger : `tempImagePath` lie a son auteur (`faceref_<userId>_<hex aleatoire>.jpg` a la generation, prefixe verifie en 403 a l application) ; `GET`/`PATCH /api/profiles/:id` et `GET /api/profiles/:id/content` (n importe qui pouvait lire -- tokens Instagram compris -- ou modifier le profil d un autre compte) : session + propriete du profil (404 si autre compte) ; `PATCH /api/profiles/:id/face-ref` (ecrivait une URL arbitraire que le pipeline telecharge = risque SSRF) : SUPPRIME apres verification qu il ne servait plus (le front ne l appelait qu a la creation d un profil, remplace par `/api/upload/face-ref` des le commit `c9f9186` ; aucun autre projet ne l appelle, sassify n utilise que `video-jobs`) ; `POST /api/generate/optimize-prompt` (appel Claude payant) et `GET /api/jobs/:jobId` : session ; `GET /api/health/schema` et `/api/health/pipeline` (ce dernier listait les contenus, profils et erreurs de TOUS les comptes) : admin uniquement (plus d acces sans session, aucun outil du depot ne les interrogeait). Controles OK sans changement : instagram/twitter/brands/delete/widget-asset/video/resolve (deja scopes par `userId`), `scheduler/run` (secret), routes `auth/*` (publiques par nature). Eprouve : 401 sans session sur chacun, 200 sur ses propres profils, 404 sur un profil inconnu, 403 sur un `tempImagePath` d un autre, pages calendrier/fiche persona/contenus toujours fonctionnelles. Non teste faute de second compte reel : l acces a un profil d un AUTRE compte existant (meme requete `userId`).
- REGLE de code (decouverte pendant ce balayage) : un `$fetch` brut execute pendant le rendu serveur (`watch` avec `immediate`, `await` au niveau setup) n envoie PAS le cookie de session ; des qu un endpoint devient protege, il faut `useRequestFetch()` (comme `useAuthSession`) ou `useFetch`, sinon la page tombe en "401 - Authentification requise" (cas reel : `calendar.vue`, corrige ; `content.vue` corrige aussi).
- [x] Upload direct d une fiche reference, reserve aux admins (2026-09-21, non commite) : `POST /api/profiles/:id/face-ref-upload` (`server/api/profiles/[id]/face-ref-upload.post.js`) -- session obligatoire, admin obligatoire, profil du compte uniquement, JPG/PNG verifie par signature du fichier, 10 Mo max, derive le `hairPrompt` comme le flux existant. Les admins sont les emails de `ADMIN_ACCOUNTS` (`server/utils/adminAccounts.js`, sans colonne en base) ; `resolveAuthUser` expose `isAdmin`, et la fiche persona affiche la zone "Importer directement une fiche 3 vues" seulement pour eux. Eprouve en navigateur (upload valide 200, faux fichier 400, vide 400, profil inconnu 404) ; le refus 403 pour un non-admin n est verifie que par la logique (tests unitaires de la liste), pas en navigateur. ATTENTION : `ADMIN_ACCOUNTS` doit aussi etre posee dans le `.env.local` du VPS Plotline, sinon la zone n y apparaitra pas.
- [ ] A trancher (pas decide) : quotas de generation de fiches par plan. Idee evoquee : generation de fiches illimitee pour le plan le plus cher, plafonnee pour les autres, galerie des fiches generees, invitation a monter de plan. Prerequis absents : `User.plan` (SOLO/STUDIO/AGENCY) est purement cosmetique (aucune facturation, aucun compteur d usage), les candidates de comparaison n existent que cote client (une galerie demande une table persistante et l exclusion de ces fichiers du script de nettoyage des orphelins), ne pas decompter les generations ratees. Point de cout : une fiche = une image Gemini (quelques dizaines de centimes), les videos (surtout longues, 4 generations) sont le vrai poste a plafonner. Ordre conseille : upload admin, puis facturation + compteur d usage, puis galerie/quotas.
- [ ] Ajouter mode dry-run + notifications d'erreur + logs structures
- [ ] Deployer le chainage video long (commite sur `main`, pas encore pousse/deploye) ET le mode "long" corrige de sassify, DANS CET ORDRE : Plotline d abord, sassify ensuite. Test reel OK (2026-09-21) sur le vrai article "Timide, reserve..." (pilier `process`) : script genere avec le mode long de sassify -> 73 mots, 4 phrases, 0 mot tronque -> 4 segments Omni Flash chaines -> video de 40s (h264/aac 720x1280, ~327s de generation, ~4 generations), personnage et decor du pilier coherents du debut a la fin ; le son/lip-sync n a pas pu etre ecoute par l agent. Constat AVANT correction : l ancien mode "long" de sassify (90-140 mots, 9 phrases) ne gardait que 61 mots sur 138 et perdait la conclusion/l appel a lire l article. Modification faite dans `sassify/scripts/generate-video-script.cjs` (repo sassify, NON commite, NON pousse) : mode long = exactement 4 phrases de 18-24 mots (70-96 mots), la 4e etant la chute ; l avertissement obsolete "10s seulement" est remplace par un avertissement si le modele produit plus de 4 phrases. `PLOTLINE_INFLUENCER_ID` a ete ajoutee sur le VPS sassify le 2026-09-17 (elle bloquait tout appel a Plotline). La video de test a ete rangee dans les creations de la marque Sassify (compte contact@sassify.fr, statut PENDING) : a supprimer depuis l interface si inutile.
- [x] Parametre `influencerId` de `/api/external/video-jobs` renomme `profileId` (2026-09-21, commite et deploye : `EXTERNAL_VIDEO_JOBS_API_KEY` et `ADMIN_ACCOUNTS` verifies presents sur le VPS Plotline le 2026-09-22) : c'est le profil qui RECOIT la video (dossier de rangement, jamais la personne a l'ecran, `withFaceRef: false`). `influencerId` reste accepte (ancien nom, encore envoye par le sassify deploye) ; la colonne en base `GeneratedContent.influencerId` ne change pas. Cote sassify (repo separe, non commite) : variable `PLOTLINE_PROFILE_ID` (l'ancienne `PLOTLINE_INFLUENCER_ID` reste lue en repli) et corps de requete envoyant les deux noms le temps du deploiement ; a retirer `influencerId` du corps une fois Plotline deploye. Pipeline reel verifie sur le VPS : cron lundi 9h -> article `brouillon` + Telegram -> clic "publier" -> `telegram-bot.cjs` lance `generate-video-script.cjs` -> job Plotline (voir sassify/CLAUDE.md).
- [ ] A trancher : destination des videos de blog envoyees par `/api/external/video-jobs`. Aujourd hui elles vont dans les creations du persona "Jeremie" (`PLOTLINE_INFLUENCER_ID` = `cmtr3k1tt0000u4n2ws3oxwmr`, `brandId` vide ; 3 videos de test des 7-8 septembre y sont), alors que le personnage a l ecran est fictif. Recommandation : pointer `PLOTLINE_INFLUENCER_ID` vers la marque Sassify (`cmton85kd0002i0l1hppn29y9`, meme compte) ; la liste des contenus d un profil couvre deja `influencerId`, `brandId` ET `ambassadorId` (`content.get.js`), donc un futur mode avec persona verrouille pourrait stocker `influencerId = persona` + `brandId = marque` et apparaitre des deux cotes sans disparite.
- [x] Definition des formats du calendrier editorial clarifiee et corrigee (2026-09-22, non commite) : REEL = video avec un personnage clairement visible (persona du catalogue OU personnage fictif) ; FEED = image, le plus souvent avec personnage ; STORY = image ou video SANS aucun personnage obligatoire (decor, ambiance). Trois corrections faites en consequence :
  1. **REEL n est plus specifique a TikTok.** `platformForFormat` (`contentPlanner.js`) et les 3 autres endroits qui forcaient `platform: 'TIKTOK'` pour un REEL (`generate/image.post.js` x2, `generate/video.post.js`, `external/video-jobs.post.js`) renvoient desormais `INSTAGRAM`. Ce n etait pas qu un detail editorial : `scheduledPublisher.js` (`resolvePublishTarget`) refuse explicitement de publier automatiquement tout contenu marque TIKTOK (aucune route de publication TikTok n existe, decision deja actee), et Instagram publie deja les Reels nativement (`instagramPublisher.js`, `media_type: 'REELS'`). Tout REEL genere par la cadence, le Studio ou `/api/external/video-jobs` (videos de blog sassify) etait donc jusqu ici condamne a ne jamais pouvoir etre publie automatiquement, ni meme manuellement depuis l app.
  2. **Bug reel corrige : une Story generee depuis le calendrier plantait a 100%** (`No Pinterest video found for query: undefined`). `approve.post.js` envoyait un STORY au pipeline Pinterest (`generationWorker.js`, qui traite TOUJOURS une Story comme une recherche Pinterest, quel que soit le `workflowType`) sans jamais fournir de mot-cle -- `ContentPlanItem` n a meme pas de colonne pour ca. Corrige sans migration : nouveau `server/utils/pinterestKeywordPicker.js`, qui pioche un mot-cle a la generation (pas a l ecriture du plan) directement dans la banque deja qualifiee `server/data/variables.json` (`pinterest_video_tags_story`/`_reel`, format "video + 2-3 mots precis"), plutot que de laisser Claude en inventer un a la volee (un mot-cle libre mal choisi peut ne renvoyer aucun resultat). La categorie (lifestyle/beach/outfit/...) est devinee depuis la niche/le style texte libre du profil, repli sur lifestyle.
  3. **Reel de mauvaise qualite dans la cadence, corrige par routage plutot que par nouvelles gardes.** `approve.post.js` envoyait tout REEL vers `resolveVideoModelOrThrow`/`runVideoGenerationJob` (choix Veo/Kling par mots-cles dans le prompt, jamais Omni Flash automatiquement, aucune garde qualite) -- un pipeline concu pour les videos de blog sassify (personnage invente librement), pas pour un persona du catalogue. Constat : dupliquer les gardes qualite de `runReelWorkflow` (rejet des clips Pinterest trop courts/boomerang/sans visage-haut du corps visible, jusqu a 3 tentatives, puis Kling Motion Control sur le mouvement reel du clip retenu) sur ce second pipeline n avait pas de sens, puisque ce pipeline ne part d aucune video source a filtrer -- il genere du texte vers video de zero. Le vrai probleme etait un mauvais routage, pas des gardes manquantes. Desormais, `approve.post.js` route par presence d une face ref : profil avec face ref (persona du catalogue) -> `runReelWorkflow` via `processGenerationJob` (memes gardes que le flux manuel Studio, mot-cle Pinterest via le nouveau picker). Profil sans face ref (personnage fictif) : **premiere version (forcer Omni Flash pour tout personnage fictif) corrigee le meme jour** apres remarque justifiee de l utilisateur -- Omni Flash n a de sens que s il y a effectivement une levre a synchroniser, pas par defaut. `extractQuotedDialogue` (`videoModelSelector.js`) repere une repartie entre guillemets dans l idee : s il y en a une, Omni Flash (seul modele du projet avec lip-sync natif) avec la repartie extraite en `dialogueText` et le reste en `scenePrompt` ; sinon, `selectVideoModel` (Kling pour du mouvement pur, Veo pour du cinematique/lent -- deja utilise ailleurs dans le projet), jamais Omni Flash pour du contenu qui ne parle pas. La seule contrainte dure qui reste : Kling Motion Control est techniquement impossible sans face ref (il anime une image de personnage generee a partir d elle), donc hors de portee pour un personnage fictif quel que soit le contenu. 69 -> 74 tests (2 mis a jour pour la plateforme, 6 nouveaux : picker de mots-cles + `extractQuotedDialogue`).
- [x] 4e valeur de Format Rotation `CUSTOM_PROMPT_STUDIO` (2026-09-22/23) : genere un contenu a partir de la base de prompts qualifiee de l utilisateur (`server/data/widgets.js`) plutot que d une recherche Pinterest -- objectif a terme : reduire la dependance a Pinterest, en comparant les deux sources. Migration appliquee sur Neon par l utilisateur le 2026-09-23 (tentative d application directe via le MCP Neon refusee par le classificateur automatique, categorie "Production Deploy", meme avec autorisation explicite -- reste une action manuelle), `schema.prisma` et client Prisma regeneres. Selectionnable dans `SUPPORTED_PLAN_FORMATS` mais **absent de `DEFAULT_FORMAT_ROTATION`** : experimental, a ajouter a la main a la cadence d un profil pour le tester, jamais impose silencieusement. Option B retenue (une 4e valeur de format autonome plutot qu un widget rendu selectionnable) : reutilise le mecanisme "idee -> champs" deja existant (`generateWidgetFields`, `widgetFieldsAssistGenerator.js`) avec l idee ecrite par Claude pour ce creneau comme matiere premiere, puis `resolveWidgetPrompt` (`widgetEngine.js`) pour le texte final -- aucun nouvel appel Claude dedie. Champ des widgets automatisables volontairement restreint a 2 sur 5 (`chooseCustomPromptWidget`, `server/utils/customPromptStudioRouting.js`), choisi selon la presence d une face ref : PORTRAIT_STUDIO (persona, IMAGE, generation classique) sans face ref -> SCENARIO_BLOG (pas de persona, VIDEO, Omni Flash sans face ref -- exactement le chemin deja valide par `/api/external/video-jobs`). FOOD_AD et UGC_PRODUIT exclus (exigent un asset uploade, absent a l approbation automatique d un plan) ; VLOG_LIFESTYLE exclu (persona + Omni Flash deux tours, le seul chemin qui verrouille l identite ET fait parler -- connu pour deriver sur l identite, voir decision du 2026-09-09 -- pas encore eprouve en generation automatisee). 74 -> 75 tests.
- [ ] A explorer (pas decide, faisabilite inconnue) : integrer du B-roll dans les videos generees -- des plans d illustration sur ce qui est dit, pas seulement le personnage qui parle a l ecran en continu. Idee de l utilisateur (2026-09-22) : B-roll pendant que le personnage parle (coupe visuelle sur l illustration, voix off continue), et/ou juste avant/apres la scene avec personnage. A verifier avant tout code : Omni Flash genere un clip unique par appel (voix + image lies), donc un plan B-roll muet nécessiterait probablement un second appel de generation (image ou courte video sans dialogue) puis un montage/assemblage (le projet a deja `videoConcat`-like via ffmpeg pour la video longue, voir `server/utils/ffmpegBinaries.js`) plutot qu un seul appel modele ; determiner si la voix doit continuer par-dessus (audio du segment personnage conserve, piste video remplacee) ou si c est un plan totalement separe. Impact cout : au moins un appel de generation supplementaire par plan B-roll.

### Ecarte pour l instant :
- Seedance: credits prepayes epuises et generation jamais aboutie. Hors service via `SEEDANCE_ENABLED`, code conserve. Veo et Kling suffisent.
- Publication TikTok automatique: la plateforme favorise les comptes qui postent depuis l application, une publication par API est contre-productive.
- Validation Telegram: remplacee de fait par le flux in-app PENDING -> Valider -> Publier.

---

## Blocages et Points d Attention
- Le dossier cible initial n'etait pas vide; un sous-dossier plotline etait deja initialise. Le projet actif est a la racine actuelle.
- La coherence visuelle repose sur un identity lock par reference fixe, sans seed/fine-tune.
- Aucune publication automatique sans validation humaine explicite. C est une regle d etape, pas un principe definitif: l automatisation pourra etre ouverte plus tard, mais uniquement sur decision explicite du proprietaire du produit, apres un volume de publications manuelles jugees majoritairement de qualite. Aucun seuil automatique, aucune proposition du produit, et aucun agent ne leve cette regle de sa propre initiative. Elle est appliquee dans le code par `scheduledPublisher`, qui ne publie que les contenus VALIDATED.
- Toujours separer clairement generation et publication.
- Les migrations ne sont JAMAIS executees depuis le projet: on ecrit le SQL dans `prisma/migrations/`, et il est passe a la main via l'editeur SQL de Neon. Le port 5432 est bloque sur le reseau, la connexion passe par WebSocket (`@prisma/adapter-neon`, port 443).
- Ne jamais ajouter un champ scalaire au schema Prisma avant que la migration soit appliquee: le client selectionne toutes les colonnes du modele et toutes les lectures cassent. Voir tasks/lessons.md.
- Le workflow Pinterest est interdit aux comptes BRAND et CONTENT_CREATOR.
- Un compte INFLUENCER_CREATOR se lit comme une agence d influenceurs virtuels: beaucoup d influenceurs, peu de marques.
- Le rattachement marque <-> ambassadrice est volontairement limite a un seul compte. On ne relie jamais deux profils de comptes differents.
- Aucune generation media ne doit etre attendue dans une requete HTTP: reponse immediate en `processing`, suivi par polling.
- Les binaires ffmpeg/ffprobe sont resolus a l EXECUTION (`server/utils/ffmpegBinaries.js`), jamais importes statiquement: npm bloque les scripts d installation, et un import statique faisait echouer tout le build sur le serveur. Ordre de resolution: `FFMPEG_PATH`/`FFPROBE_PATH`, puis le paquet npm, puis le binaire systeme. ffmpeg ne sert qu au workflow Reel Pinterest.
- Le cookie de `state` OAuth Google (`GOOGLE_STATE_COOKIE`) n'est pas isole par port: deux projets Nuxt en local sur des ports differents partagent leurs cookies et se font ecraser leur `state` mutuellement. Voir `docs/recette-connexion-google.md` avant de reactiver du dev local en parallele d'un autre projet ayant aussi du Google OAuth.
- L URI de redirection Google (`server/utils/googleOAuth.js`, `pickOAuthBaseUrl`) doit toujours faire primer `BASE_URL` sur l origine de la requete: derriere le reverse proxy du VPS, lire l origine sans `xForwardedHost`/`xForwardedProto` renvoie l adresse interne du serveur, que Google refuse (`redirect_uri_mismatch`). Ne pas re-inverser cette priorite sans relire tasks/lessons.md (2026-09-03, deja fait une fois par erreur).
- `getBodyBlock(silhouette, gender, topGarment)` (`server/utils/injectBody.js`) prend le genre en 2e argument depuis le 2026-09-06. Toute nouvelle fonction qui l appelle doit passer le genre du profil, sinon la description corporelle retombe au feminin par defaut.
- `POST /api/external/video-jobs` (branche `feature/external-video-jobs`) est un point d entree service-a-service sans session, protege par `EXTERNAL_VIDEO_JOBS_API_KEY` (cle partagee inventee, a generer et poser identiquement sur le VPS Plotline et sur l appelant). Si la variable est absente, l endpoint refuse tout en 503 plutot que de s ouvrir sans protection -- ne jamais retirer cette garde. Ce endpoint ne fait volontairement aucun overlay texte: les videos qu il produit doivent rester des videos "normales" (type pub), sans texte par dessus.
- Le suivi de statut existant (`GET /api/content/:id/status`) exige une session utilisateur (`requireAuthUser`) et scope la requete par `userId`: inutilisable par un appelant service-a-service. D ou `GET /api/external/video-jobs/:id/status`, un jumeau sans scoping utilisateur, protege par la meme cle partagee. Trouve et corrige en testant `POST /api/external/video-jobs` en conditions reelles -- sans lui, home.sassify.fr n aurait eu aucun moyen de savoir quand sa video est prete.
- Pour ce meme endpoint externe, l identite du persona n est plus verrouillee par defaut (decision du 2026-09-09) : sur Omni Flash, verrouiller l identite via une image de depart casse le lip-sync (voix off), et le flux a deux tours qui le repare derive sur l identite et casse en debut/fin de clip -- non corrige par ajustement de prompt. Pour du contenu qui n a pas besoin de continuite de personnage (illustrer un article de blog), le modele invente desormais librement qui apparait a l ecran : plus fiable, moins cher. Le flux a deux tours reste dans le code (`requestOmniFlashTwoTurn`), disponible des qu une image de depart est fournie -- utile pour du contenu Plotline classique ou l identite doit rester verrouillee malgre ces defauts connus.
- Le dossier de travail de ce projet est potentiellement partage avec l utilisateur (qui commite et change de branche depuis son propre terminal) ou une autre session Claude Code, pas seulement avec la session en cours. Un `git checkout`/`git stash` externe peut survenir pendant une implementation et faire disparaitre des fichiers non commites du disque sans prevenir -- voir tasks/lessons.md (2026-09-10) avant de conclure a une perte de donnees : verifier `git stash list`/`git reflog` en premier, jamais re-ecrire a l aveugle.
- Le tick du scheduler de publication (`server/plugins/scheduler.ts`) et les intervalles du worker BullMQ (`server/utils/generationWorker.js`, `drainDelay`/`stalledInterval`) tournent 24h/24 des que le process est en vie, independamment du trafic reel. A la frequence par defaut (60s / 30s), ce bruit de fond a lui seul epuise le quota mensuel gratuit de Neon (CU-hours, la base ne peut jamais se mettre en veille) et d Upstash Redis (commandes). Passes a 10 minutes et 5 minutes respectivement (2026-09-13/14) -- un job reel reste pris en charge immediatement, seules les relances a vide sont espacees. Ne jamais reprendre un intervalle par defaut d une lib (BullMQ, cron) sans se demander a quelle frequence ca interroge un service externe facture/quota a l usage.
- Sur la machine de dev Windows utilisee pour ce projet, `http://localhost:<port>` peut repondre `426 Upgrade Required` sur un serveur Nuxt qui fonctionne pourtant tres bien -- `http://127.0.0.1:<port>` fonctionne normalement. Cause non elucidee (probablement une resolution `localhost` -> IPv6 qui ne tape pas le meme bind que le serveur). Utiliser `127.0.0.1` pour toute verification navigateur en local sur cette machine si `localhost` renvoie une erreur inattendue.
- Plusieurs routes de `server/api/profiles/` exportent encore via `module.exports = defineEventHandler(...)` (style CJS) plutot que `export default`. Rollup (`npm run build`) tolere le melange avec un `import` ESM statique ajoute en tete de fichier, mais le serveur de dev (Nitro/Vite) non: la presence d un `import` force Node a charger tout le fichier en ESM strict, ou `module` n existe pas, et l exception casse l enregistrement de TOUTES les routes, pas seulement celle du fichier fautif (`Cannot access 'renderer' before initialization`). Avant d ajouter un `import` statique a un fichier existant de ce dossier, verifier son style d export (`grep module.exports`) et le convertir en `export default` au meme moment si besoin -- voir tasks/lessons.md (2026-09-16).

---

## Decisions Prises
| Date | Decision | Raison |
|------|----------|--------|
| 2026-05-26 | Utiliser un systeme d'identity lock (reference visuelle fixe + prompts contraints) | Garantir une coherence persona stricte sur tous les contenus |
| 2026-05-26 | Pinterest est une source d'inspiration, jamais une sortie publiee | Maitriser la cohérence et reduire le risque legal/editorial |
| 2026-05-26 | Multi-persona natif avec persona actif switchable | Permettre la gestion de plusieurs identites dans un meme SaaS |
| 2026-05-26 | Human-in-the-loop obligatoire via validation Telegram | Garder un controle editorial avant publication |
| 2026-05-26 | Separation generation/publication | Resilience operationnelle et ouverture future a des roles (editeur/client/approbateur) |
| 2026-05-26 | Conventions serveur: logique metier dans server/utils, endpoints dans server/api | Maintenir un codebase clair et testable |
| 2026-08-08 | L etat courant reste sur GeneratedContent, l historique vit dans ContentVersion | Ne pas toucher au code de publication et de listing pour ajouter le retour arriere |
| 2026-08-08 | Un media n est supprime que s il n est reference ni par le rendu courant ni par une autre version | Le retour arriere copie l URL a deux endroits: purger sans verifier casserait le contenu actif |
| 2026-08-09 | Le format d image/video est decide par le prompt; le repli 9:16 ne s applique que si le prompt est muet | Ne jamais imposer un cadrage a la place de l auteur |
| 2026-08-09 | Une slide de carrousel reste un contenu autonome, regroupe par carouselId | Permettre de modifier une seule slide sans regenerer les autres |
| 2026-08-10 | Aucun appel de generation media n est attendu dans une requete HTTP | Les fournisseurs prennent plusieurs minutes: la requete serait coupee par le proxy avant la fin |
| 2026-08-10 | Le workflow Pinterest est reserve aux comptes influenceur | Il n a pas de sens editorial pour une marque ou un createur de contenu |
| 2026-08-10 | Une ambassadrice peut representer plusieurs marques (table BrandAmbassador) | C est la realite du metier, et une ambassadrice est un actif coûteux qu on ne veut pas recreer par marque |
| 2026-08-10 | Le type de profil est ecrit en base au lieu d etre devine depuis la face ref | L inference cassait silencieusement des qu une marque aurait eu une face ref |
| 2026-08-10 | Un plan editorial est relu avant que le moindre media soit produit | Valider un texte coute une requete Claude, valider apres generation coute autant de videos que d idees |
| 2026-08-10 | La cadence est deterministe, Claude n ecrit que le texte des idees | Le rythme doit rester previsible meme quand l IA est indisponible |
| 2026-08-14 | OAuth Google implemente a la main (~150 lignes, aucune dependance) plutot qu avec un module tiers (nuxt-auth-utils et consorts) | Un module tiers installe sa propre session en parallele de celle du projet: deux mecanismes d authentification, dont un seul est revocable |
| 2026-08-14 | Rattachement automatique d un compte Google a un compte mot de passe existant, uniquement si Google declare `email_verified: true` | Rattacher sur la seule correspondance d email est une prise de controle de compte: n importe qui creant un compte Google avec l adresse d un tiers recupererait son compte |
| 2026-08-14 | Identification de l utilisateur Google par `sub` (googleId), jamais par l email | Un utilisateur peut changer l adresse de son compte Google; l identifier par email le rendrait inconnu du jour au lendemain et creerait un doublon silencieux |
| 2026-09-03 | La validation humaine avant publication est une regle d etape, pas un principe definitif | L automatisation sera ouverte quand le proprietaire du produit le decidera, apres assez de publications manuelles jugees majoritairement de qualite; ni seuil automatique, ni proposition du produit |
| 2026-09-06 | Ajout d un genre (Femme/Homme) au persona, avec silhouettes et textes de generation masculins dedies plutot qu un reemploi des silhouettes feminines | La creation de persona etait entierement pensee au feminin (placeholders, silhouettes a base de tour de poitrine); Plotline se presente comme une agence d influenceurs virtuels, pas seulement d influenceuses |
| 2026-09-06 | Le picker de silhouette (duplique dans 3 ecrans) devient un select natif alimente par une seule source (`usePersonaOptions.js`), sans badge ni coche personnalisee | Un vrai menu deroulant ne peut pas afficher de badge sur ses options; le caractere "par defaut" est indique dans le libelle lui-meme |
| 2026-09-07 | La comparaison de fiches reference visage se limite a 3 en simultane, avec remplacement de la moins bonne au-dela, plutot qu une liste illimitee | Comparer plus de 3 images a l ecran degrade l experience sans ameliorer la decision; le remplacement force un choix actif (laquelle ecarter) plutot qu une accumulation passive |
| 2026-09-07 | Plotline expose un point d entree externe (`/api/external/video-jobs`) authentifie par cle partagee plutot que par session, pour que d autres projets du meme proprietaire (home.sassify.fr) reutilisent le pipeline video existant | Reconstruire un pipeline de generation video pour un second projet aurait duplique tout l identity lock et l integration fournisseurs deja eprouves ici; le controle d acces d un outil interne n a pas besoin d un compte utilisateur |
| 2026-09-07 | L overlay texte d accroche (hookVideo), d abord code pour `/api/external/video-jobs`, en est retire avant le premier test reel | Le texte en surimpression est pense pour le format de contenu influenceur (aguicheur, explicite), pas pour les videos type home.sassify.fr qui doivent rester des videos normales sans texte par dessus; l implementer au bon endroit viendra avec ce futur chantier, pas ici |
| 2026-09-09 | Pour les videos home.sassify.fr, l identite du persona n est plus verrouillee par defaut -- le modele invente librement le personnage | Sur Omni Flash, verrouiller l identite via une image de depart casse le lip-sync ou, avec le flux a deux tours qui le repare, derive sur l identite et casse en debut/fin de clip; pour ce format sans besoin de continuite de personnage, un rendu propre et moins cher prime sur l identite exacte |
| 2026-09-09 | Le widget "Video Scenario" demande une persona pour classer le contenu, mais ne verrouille son identite que sur case a cocher explicite (desactivee par defaut) | Separer "qui possede ce contenu dans Mes creations" (obligatoire, `GeneratedContent.influencerId` est une colonne non nulle) de "l identite doit-elle etre verrouillee visuellement" (optionnel, coute cher et a des defauts connus sur Omni Flash) evite de forcer une contrainte d identite quand elle n apporte rien |
| 2026-09-09 | La comparaison de modeles video sur le widget "Video Scenario" reutilise l historique de versions existant plutot qu un nouveau systeme de candidats multiples | L utilisateur avait deja construit ce mecanisme (regenerer avec un autre modele -> nouvelle `ContentVersion` avec son `generationModel`, navigable et selectionnable dans le modal existant) -- le reconstruire aurait duplique une fonctionnalite deja eprouvee |
| 2026-09-10 | Un seul mecanisme generique "idee -> Claude remplit les champs" pour les 5 widgets Studio, plutot qu un generateur dedie par widget | Les templates de widgets viennent de la distillation de plusieurs bons prompts reels sur une thematique -- ce qui compte est que chaque champ garde sa consigne propre (portee par un `assistHint` dans `widgets.js`), pas le mecanisme de generation lui-meme ; un seul systeme generique evite de dupliquer l appel Claude/le parsing JSON a chaque nouveau widget |
| 2026-09-10 | L assistant "idee -> prompt" reste sur l API Anthropic plutot que de passer par un compte Claude personnel (copier-coller) | Le cout d un appel texte seul (quelques centimes) est negligeable face aux videos generees (~1-2$) qui motivaient la prudence budgetaire de la session precedente ; aucune integration automatisee n est possible avec un compte personnel depuis un backend SaaS |
| 2026-09-16 | Un persona/profil n est plus obligatoire pour generer du contenu, via un profil `ACTIVITY` generique auto-cree par compte plutot qu un `influencerId` nullable en base | Certains contenus (personnage fictif du widget Video Scenario, affiche produit generique) n ont pas de persona du catalogue a leur attribuer ; rendre la colonne nullable aurait aussi exige de construire une vue "contenu sans profil" dans Mes creations, qui n existe pas aujourd hui (la navigation y est entierement scopee par profil) -- le profil generique est un detour beaucoup plus rapide et sans migration qui garde ce contenu normalement visible et retrouvable |
| 2026-09-16 | Le reglage de la cadence editoriale vit sur la fiche persona plutot que sur un ecran dedie, et un plan `DRAFT` apparait sur le calendrier mensuel avec un style visuellement distinct | La cadence est un attribut du profil, pas une fonctionnalite a part entiere -- l exposer la ou le profil se configure deja evite un nouvel ecran de navigation ; et un plan pas encore approuve n engage aucun credit, donc l afficher sur le calendrier (plutot que de le cacher dans un ecran de revue isole) aide a visualiser le rythme reel avant meme d approuver quoi que ce soit |

---

## Notes de Session
> Ajouter ici un resume a la fin de chaque session de travail.

- 2026-05-26: Cadrage produit saisi (identity lock, scheduler, pipeline Pinterest, tags, architecture), projet Nuxt initialise, Prisma initialise, branche principale renommee en main.
- 2026-06-07: Ajout du workflow story video Pinterest, stockage local des MP4, lecture media .mp4 et publication Instagram video avec polling du container.
- 2026-06-10: Healthcheck schema corrige (cast `information_schema.column_name`), 3 jobs `PROCESSING` orphelins marques `FAILED`, fallback Gemini durci avec retries transitoires et sanitisation plus large.
- 2026-07-01: Refond du back office avec shell global sidebar, dashboard bento, calendrier mensuel, vue contenu 3 colonnes, hook de notifications jobs actifs et routes schedule/active.
- 2026-07-30: Ajout du lien explicite `campaignId` sur les contenus, filtres campagne sur dashboard/contenu, suppression de campagne avec detachement des contenus, et panneau ambassadrice repliable dans le Studio.
- 2026-08-10: Sprints historique + carrousel + fiabilisation video. Historique des versions avec retour arriere sans perte de media, correction de la regeneration destructive, nettoyage des medias a la suppression. Regroupement des slides de carrousel avec navigation et modification isolee. Format decide par le prompt au lieu d'un 9:16 en dur. Seedance rebranche sur la vraie API seevio (tache asynchrone, image de depart par URL Blob). Les trois fournisseurs video passent en tache de fond: Kling bloquait la requete HTTP jusqu'a 15 minutes. Rapatriement ponctuel de 6 medias vers le Blob et script de nettoyage des orphelins. Colonne morte `GeneratedContent.generationModel` supprimee par migration.
- 2026-08-11 / 08-12: Fiabilisation du planificateur et du stockage. Les modifications d une idee sont desormais enregistrees avant de lancer la generation: une scene reecrite puis envoyee directement partait avec l ancien texte et brulait des credits pour un contenu abandonne. Correction d une recidive du bug de stockage local: la route d upload de face ref avait deux branches, celle du pipeline de generation ecrivait toujours sur le disque sans consulter le Blob, rendant la face ref introuvable depuis tout autre environnement. Script `scripts/migrate-face-refs-to-blob.cjs` ajoute pour rapatrier l existant.
- 2026-08-14: Connexion et inscription Google ajoutees (echange OAuth a la main, `googleId` nullable/unique sur `User`, rattachement automatique si `email_verified`). Documentation complete ecrite en recette reutilisable (`docs/recette-connexion-google.md`) pour reappliquer le meme flux sur un autre projet: pieges de la console Google, du cookie `state` partage entre ports en local, et effet de bord du parcours d'inscription enfin exerce. Correction d'une page d'onboarding qui referencait un middleware nomme `auth` inexistant (le seul middleware du projet est global, `auth.global.ts`, non appelable par son nom) — la protection etait deja assuree par le middleware global, la ligne fautive a simplement ete retiree.
- 2026-09-03: Connexion Google eprouvee en production. Correction de la construction de l URI de redirection, qui partait de `BASE_URL` avec un repli silencieux sur localhost: la variable etant absente du VPS, la production renvoyait les utilisateurs vers leur propre machine (`ERR_CONNECTION_REFUSED`), en laissant croire a une mauvaise configuration Google. Premiere correction: l origine de la requete prime sur `BASE_URL`. Modale d onboarding conditionnee au chargement effectif de la liste des profils: elle s ouvrait sur un compte pourtant pourvu de trois profils. Toast de bienvenue supprime avec son composable. Arret du worker BullMQ sur erreur definitive, apres une inondation de logs due au quota Redis epuise.
- 2026-09-03 (suite): Deux deploiements echoues ont laisse la production hors service. Le workflow supprimait `.output` AVANT de construire: un build rate detruisait donc la version en ligne au lieu de la laisser intacte. Le build est desormais realise a cote, l ancienne sortie n est supprimee qu apres succes et restauree en cas d echec, et `git fetch` est retente trois fois (une limitation ponctuelle de GitHub avait fait rater un deploiement). Cause du build rate lui-meme: npm bloque les scripts d installation non autorises, donc le binaire de `ffmpeg-static` n etait plus telecharge, et Nitro echoue en le tracant.
- 2026-09-03 (re-suite): la premiere correction Google du jour a provoque une recidive, `redirect_uri_mismatch` cette fois: derriere le reverse proxy du VPS, l origine de requete sans en-tetes forwarded vaut l adresse interne du serveur, pas `plotline.sassify.fr`. `BASE_URL` (deja posee sur le VPS) refait foi en priorite, l origine de requete n est plus qu un repli lisant desormais `X-Forwarded-Host`/`X-Forwarded-Proto`. Un log trace l URI de redirection envoyee pour rendre tout futur mismatch immediat a diagnostiquer. Un helper `isHttpUrl()` reference mais efface lors d une edition precedente a ete restaure au passage.
- 2026-09-05 (branche `fix/seo-meta-titles`): Correction SEO. `robots.txt` autorisait l exploration de tout le site alors que la quasi-totalite des pages exige une connexion: Google avait indexe des URLs qui ne menent qu a l ecran de connexion (repere via un lien `/_nuxt` -- dossier d assets techniques, jamais une page -- indexe puis redirigeant vers `/auth/login`). `robots.txt` interdit desormais l exploration de tout ce qui est derriere l authentification, ne laissant que l accueil, la connexion et l inscription. Seule la page d accueil posait un titre (`useSeoMeta`); toutes les autres remontaient "Sans titre" dans les resultats de recherche -- un titre de repli global ("Plotline") est ajoute dans `nuxt.config.ts` (`app.head.titleTemplate`), et connexion/inscription recoivent chacune un titre complet. Titre de l accueil corrige au passage: melangeait anglais et francais ("Face Consistency automatique pour influenceuses IA").
- 2026-09-06 (branche `feature/persona-gender`, fusionnee sur `main`): Ajout du genre a la creation/edition de persona. Nouvelle colonne `Profile.gender` (`FEMALE`/`MALE`, defaut `FEMALE` pour ne rien changer aux personas existantes) et deux nouvelles valeurs d enum `SilhouetteType` (`MUSCULAR`, `STOCKY`). `injectBody.js` genere desormais un texte corporel reellement masculin (epaules, torse) pour les personas homme, au lieu de reutiliser les descriptions feminines existantes. Le picker de silhouette, duplique a l identique dans 3 ecrans (creation, edition, onboarding marque), est remplace par un select natif alimente par une seule source (`usePersonaOptions.js`). Question ouverte, non tranchee: ajouter une option non-binaire (extension naturelle du meme systeme) et/ou une option "mascotte" (produit different: pas un genre humain, pas de silhouette applicable, chantier separe si retenu).
- 2026-09-06 (suite, branche `feature/studio-widgets`): la branche des widgets Studio, mise en pause avant le travail sur le genre, a ete rebasee sur `main` (fast-forward propre, deux conflits mineurs resolus a la main dans `edit.vue` et le fichier de tests -- les deux fonctionnalites ajoutaient chacune leur propre bloc au meme endroit). `server/utils/personaDescription.js`, ecrit avant le changement de signature de `getBodyBlock`, appelait encore l ancienne forme -- corrige, et un second appel manquant trouve au passage: `server/api/widgets/resolve.post.js` ne selectionnait pas encore `gender` en base.
- 2026-09-07: Bug critique trouve et corrige en production -- impossible de creer une persona, la page devenait blanche au recapitulatif final sans message d erreur. Cause: l ajout du genre a transforme `silhouetteOptions` en `computed()`, mais `selectedSilhouetteLabel` appelait encore `.find()` dessus sans `.value` -- un computed n est pas un tableau, l appel plantait avant meme que le bouton de creation soit atteignable. Corrige sur `main` et propage sur `feature/studio-widgets` qui avait herite du meme bug via sa fusion. Puis, sur une nouvelle branche `feature/face-ref-comparison`: possibilite de comparer plusieurs fiches reference visage (jusqu a 3, generees a partir de photos source differentes) avant de choisir laquelle utiliser, avec remplacement de la moins bonne au-dela de 3 -- aucun changement serveur, l endpoint de generation existant est simplement appele plusieurs fois cote client.
- 2026-09-07 (suite, branche `feature/external-video-jobs`): premiere brique de l integration avec home.sassify.fr (meme proprietaire, projet separe), qui genere desormais des scripts video a partir de ses articles de blog et doit les faire produire par le pipeline video de Plotline. Verification prealable demandee par l utilisateur avant tout code: aucune notion de "type de video" n existait au-dela du format REEL/STORY code en dur, et aucune trace du modele "Omni Flash" nulle part dans le depot -- confirme par une recherche texte complete avant d ecrire quoi que ce soit. Une fois les vraies specs du modele fournies (`gemini-omni-1.1-flash`, disponibilite generale au 27/08/2026, appele via l Interactions API de `@google/genai` et non `generateContent`), ajout de `POST /api/external/video-jobs` (authentification par cle partagee `EXTERNAL_VIDEO_JOBS_API_KEY`, aucune session), le modele force sur Omni Flash pour ces jobs uniquement. Un overlay ffmpeg drawtext du hookVideo avait d abord ete ajoute puis retire dans la meme session: l utilisateur a precise que le texte en surimpression est pense pour le format de contenu influenceur, pas pour ces videos type home.sassify.fr qui doivent rester normales, sans texte -- le module `videoTextOverlay.js` a ete supprime avant le premier test reel.
- 2026-09-07 (re-suite): premier test reel de `POST /api/external/video-jobs` en local contre la vraie base Neon. Reussi du premier coup: video Omni Flash generee (2,24 Mo, mp4 valide) et uploadee sur le Blob partage. Au passage, decouverte et correction d un vrai trou avant meme la fusion: `GET /api/content/:id/status` exige une session utilisateur, donc inutilisable par home.sassify.fr pour suivre son propre job -- ajout de `GET /api/external/video-jobs/:id/status`, jumeau sans session, protege par la meme cle partagee. Le test a aussi mis en evidence un serveur de dev local reste bloque depuis une session precedente (process toujours actif mais ne repondant plus): tue et redemarre proprement.
- 2026-09-08/09 (branche `feature/scenario-widget`): identity lock d Omni Flash eprouve en conditions reelles sur home.sassify.fr -- image de depart + demande de parole en un seul appel verrouille l identite mais casse le lip-sync (voix off). Investigation : la doc officielle et un test reel confirment que le lip-sync ne passe que par le mecanisme documente "multi-turn voice extension" (`previous_interaction_id`), qui etend une interaction VIDEO precedente, pas une simple reference image. Flux a deux tours implemente (`requestOmniFlashSingleTurn`/`requestOmniFlashTwoTurn`) : tour 1 scene silencieuse, tour 2 meme interaction etendue avec la parole. Fonctionne pour l identite et le lip-sync, mais sur plusieurs tests reels l identite derive d un tour a l autre et des artefacts de raccord audio/bouche apparaissent en debut/fin de clip -- non corriges par ajustement de prompt. Decision : pour les videos home.sassify.fr (pas de compte influenceur, pas de besoin de continuite de personnage), abandon du verrouillage d identite par defaut, retour au mode texte-seul (deja fiable, moins cher). Le flux a deux tours reste dans le code pour un usage futur ou l identite compte davantage. `feature/studio-widgets` fusionnee dans cette branche pour construire un widget Studio "Video Scenario" (idee -> script redige par Claude -> generation, persona avec verrouillage d identite optionnel) -- reutilise l historique de versions existant (`ContentVersion.generationModel`, modal "Version X/Y") pour comparer les modeles au lieu de construire un nouveau systeme de comparaison.
- 2026-09-09 (re-suite) : widget "Video Scenario" construit (`server/data/widgets.js`, `server/utils/scenarioScriptGenerator.js`, `server/api/widgets/scenario-script.post.js`), `dialogueText` file jusqu a `runVideoGenerationJob` depuis `/api/generate/video` et `/api/content/:id/regenerate`. `feature/studio-widgets` rebasee dans la meme branche (conflit resolu sur `videoGeneration.js` : les deux cotes coexistent, Omni Flash d un cote, `customReferenceImageUrl` de l autre). Plusieurs allers-retours avec l utilisateur apres verification visuelle reelle (connecte, capture d ecran) : libelles sans accents corriges, flux reordonne en etapes numerotees (idee -> profil/verrouillage -> verification) pour rendre evident que les champs scene/script se remplissent depuis le bouton "Generer le script", select de profil groupe par `profileType` (Persona/Marque/Autre), et surtout l encart sous la case "Verrouiller l identite" rendu reactif -- corrige deux fois de suite pour la meme raison (voir tasks/lessons.md 2026-09-09). Omni Flash ajoute comme option du selecteur de modele (absent jusque-la, et jamais choisi par "Automatique" qui ne le connait pas) mais reserve a ce widget. Fusionne sur `main` et deploye, verifie par sonde HTTP (`/`, `/studio`) -- aucun test reel payant sur ce widget precis a ce stade.
- 2026-09-10 (branche `feature/prompt-assist-claude`, fusionnee sur `main`) : ajout de l assistant Claude generique "idee -> prompt" evoque en fin de session precedente. Exploration prealable du code existant (flux "Prompt libre", carrousel, systeme de widgets) via 3 agents en parallele avant tout plan, pour reutiliser au maximum les patterns deja eprouves (`scenarioScriptGenerator.js`, `planIdeaGenerator.js`, `buildPersonaDescription`). Decision prise avec l utilisateur : un seul mecanisme generique pour les 5 widgets plutot qu un par widget, chaque champ portant sa propre consigne via un nouveau `assistHint` dans `widgets.js` -- l ancien generateur dedie a "Video Scenario" est retire, ses consignes exactes reportees mot pour mot dans les hints pour ne rien perdre en qualite. Nouveaux fichiers : `promptAssistGenerator.js` (prompt libre image/video), `widgetFieldsAssistGenerator.js` (champs de widget, generique), `carouselAssistGenerator.js` (slides de carrousel, Claude decide du nombre entre 2 et 10), `personaLookup.js` (extrait de `resolve.post.js` pour etre reutilise par les 3 nouveaux endpoints). Coincidence de branche en plein milieu du travail : l utilisateur (ou une autre session) travaillait en parallele dans ce meme dossier sur `fix/ambassador-gender-and-profile-fields`, et a change de branche pendant que des fichiers etaient en cours d ecriture -- ses changements de branche ont emporte le travail non commite via un `git stash --include-untracked` prudent et explicitement etiquete. Rien perdu une fois le stash retrouve et reapplique sur la bonne branche (voir tasks/lessons.md 2026-09-10), mais a fait perdre du temps de diagnostic. Merge sur `main` sans conflit (le fix de l utilisateur y etait deja), 61/61 tests verts, `npm run build` propre. Pas de test reel en navigateur ni de generation Claude reelle a ce stade (voir Prochaines etapes).
- 2026-09-13/14 (session separee) : deux fuites de quota corrigees en production, toutes deux causees par du bruit de fond 24h/24 independant du trafic reel. Le tick du scheduler de publication interrogeait Neon toutes les 60s des que `SCHEDULER_ENABLED=true`, empechant la base de se mettre en veille et epuisant a lui seul 80% du quota mensuel de CU-hours -- passe a 10 minutes. Le worker BullMQ, avec les intervalles par defaut (`drainDelay`/`stalledInterval` a 30s), interrogeait Upstash Redis en boucle meme sans job en file, epuisant le quota gratuit de commandes -- passes a 5 minutes. Dans les deux cas, un job reel reste pris en charge immediatement (la lecture bloquante de la file se reveille des qu un job y est ajoute).
- 2026-09-15/16 (branche `main` directement) : validation reelle de l assistant Claude "idee -> prompt" en navigateur (les 4 zones : prompt libre image, widget Video Scenario, carrousel, avec et sans persona verrouille) -- confirme que la description du persona ne s injecte dans le prompt genere que quand l identite est verrouillee, et que le carrousel produit bien un nombre de slides coherent avec la richesse de l idee exprimee (5 slides pour "les 5 etapes de ma routine"). En testant, question soulevee par l utilisateur : l attribution d un persona etait-elle vraiment obligatoire pour generer du contenu (cas d usage : personnage fictif du widget scenario, affiche produit generique) ? Verification : oui, `GeneratedContent.influencerId` est une colonne non nulle et les deux endpoints de generation renvoient une 400 sans elle -- et rendre la colonne nullable aurait exige de construire une vue "contenu sans profil" dans Mes creations, qui n existe pas aujourd hui (toute la navigation y est scopee par profil). Choix retenu avec l utilisateur : profil `ACTIVITY` generique auto-cree par compte (`server/utils/defaultProfile.js`), jamais propose dans les selecteurs de persona (pas de `faceRefPath`) mais normalement visible dans Mes creations. Verifie sans generation payante via un script ponctuel contre la vraie base (creation idempotente, champs corrects), puis en navigateur (bouton "Generer" du widget Video Scenario active sans aucun profil choisi). Au passage, deux frictions d environnement resolues pour permettre ces tests : le port 3000 etait occupe par un tout autre projet sur la machine, et `localhost` renvoie `426 Upgrade Required` sur cette machine alors que `127.0.0.1` fonctionne normalement (voir Blocages et tasks/lessons.md). Deux points du backlog cloturees ensuite sans depense reelle, par relecture de code plutot que par nouveau test payant : le widget "Video Scenario" (le cablage prompt/dialogue jusqu a `requestOmniFlashSingleTurn` est identique, terme a terme, au chemin deja eprouve avec de l argent reel via `/api/external/video-jobs`) et Kling image2video (`requestProviderVideo` bascule sur `generateVideoFromImageAndPrompt` des qu un `startFrame` existe, lui-meme prepare des que `withFaceRef` est vrai -- exactement le cas d une video Pinterest generee pour un persona INFLUENCER_CREATOR avec face ref, que l utilisateur confirme avoir deja vu fonctionner).
- 2026-09-16 (suite, branche `main` directement) : ecran de reglage de la cadence et liste des plans passes, dernier point du backlog identifie en debut de session. Deux decisions tranchees avec l utilisateur avant tout code : la cadence vit sur la fiche persona (pas un ecran dedie), et un plan `DRAFT` apparait sur le calendrier mensuel avec un style distinct. Cote serveur : `postsPerWeek`/`formatRotation`/`publishHour` (deja en base depuis le planificateur editorial, jamais exposes) ajoutes a `GET`/`PATCH /api/profiles/:id` en reutilisant les normalizers deja purs de `contentPlanner.js` ; nouveaux `GET /api/plans` (liste, filtrable par profil) et `POST /api/plans/:id/discard` (DRAFT -> DISCARDED), calques sur les endpoints de plan existants. Cote UI : section cadence + "Plans precedents" sur `profiles/[id]/edit.vue`, bouton "Jeter ce plan" sur l ecran de revue, fusion des idees de plan DRAFT pas encore generees dans `calendar.vue` (style pointille, clic renvoie vers la revue du plan). Bug reel trouve en testant sur le serveur de dev (absent du build de prod, qui passait sans erreur) : ajouter un `import` ESM statique en haut de `profiles/[id].get.js`/`[id].patch.js`, qui se terminaient par `module.exports = ...`, fait planter TOUTES les routes du serveur de dev Nitro (`Cannot access 'renderer' before initialization`) car la presence d un `import` force Node a traiter le fichier en ESM strict, ou `module` n existe pas -- Rollup (build prod) tolere ce melange, pas le SSR dev de Vite/Nitro (voir tasks/lessons.md). Corrige en passant les deux fichiers a `export default`. Verifie de bout en bout en navigateur, connecte sur le compte reel : cadence modifiee et persistee, plan cree (test reel, cout Claude texte negligeable) puis visible sur le calendrier avec le style brouillon et dans "Plans precedents", abandon depuis les deux endroits verifie, un second abandon refuse en 409. Plan et cadence de test nettoyes de la base reelle apres verification.
- 2026-09-17/19 : (1) Diagnostic "pas de video pour l article du lundi" : `PLOTLINE_INFLUENCER_ID` manquait dans `/srv/saas/home/.env.local` sur le VPS sassify (seule `PLOTLINE_API_KEY` y etait), donc `generate-video-script.cjs` sortait en `non_configure` sans jamais appeler Plotline ; l utilisateur l a ajoutee. Point latent restant cote sassify : `generate-article.cjs` n appelle pas `generate-video-script.cjs`, chacun depend de son propre cron VPS. (2) Test comparatif face ref (fiche japonaise 5 angles/expressions/palette utilisee telle quelle vs face ref 3 panneaux classique, meme modele `gemini-3-pro-image-preview` = "Nano Banana Pro") : le systeme actuel a mieux tenu l identite sur cet essai unique, aucun changement de code. (3) Chainage video long Omni Flash (voir "Ce qui est fait"). Deux bugs trouves en test reel payant, documentes dans tasks/lessons.md : le `timeout` de `new GoogleGenAI` etait un no-op (il faut `httpOptions.timeout`), et les interactions chainees renvoient la video cumulative. Point d attention pour le mode "long" de sassify.fr (90-140 mots vise) : le decoupage empile par PHRASE et non par mot, avec des phrases de 12 a 21 mots on n en fait tenir qu environ 4 dans 4 segments (~60 mots) -- le reste est tronque ; viser 4 phrases plutot qu un nombre de mots cote sassify.

---

## Lecons Apprises
> Voir tasks/lessons.md pour le detail des corrections et patterns a eviter.

- Respect strict de la consigne utilisateur: ne rien creer hors instruction explicite.

---

## Regle de memoire narrative
Apres toute session impliquant une decision business, un pivot, un changement de statut, ou un apprentissage terrain significatif (pas les changements purement techniques), mettre a jour /STORY.md en consequence, en plus des notes de session habituelles.
